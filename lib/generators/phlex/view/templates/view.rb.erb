# frozen_string_literal: true

module Views
  class <%= "#{namespace}::" if namespaced? %><%= class_name %> < Views::Base
    def view_template
      h1 { "<%= class_name %>" }
      p { "Find me in <%= @path %>" }
    end
  end
end